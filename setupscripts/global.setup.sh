#! /usr/bin/env bash

# This script will run just before the ZEnv install process completes. If it
# exits with a 0 exit code, the installation is considered a success.

# It's best if you only put things that _need_ to be first-time here.
# Settings that are used on a daily basis should go in the environment/
# folder instead.
